You have authenticated successfully with Google but there is no <code>success_redirect_url</code> configured.
You can configure this for your project with:<br/>
<code>
npx @firesync/server google_auth configure --project <%= projectName %> --success-redirect-url &lt;url-to-return-to-next&gt;
</code>
